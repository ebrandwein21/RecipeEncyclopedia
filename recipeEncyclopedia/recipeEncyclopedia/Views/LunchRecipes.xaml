<Window x:Class="recipeEncyclopedia.Views.LunchRecipes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:recipeEncyclopedia.Views"
        mc:Ignorable="d"
        Title="LunchRecipes" Height="600" Width="900"
        Background="Beige">
    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <Label Content="Welcome To The Lunch Recipe Hub"
               FontSize="30" FontWeight="Bold"
               HorizontalAlignment="Center" Grid.ColumnSpan="2"
               Margin="0,10,0,20" />

        <!-- Home and Exit Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Button Content="Home" Click="Home_Click" Width="75" Margin="0,0,10,0" />
            <Button Content="X" Click="ExitButton_Click" Background="Red" Foreground="White" Width="30" />
        </StackPanel>

        <!-- ListBox for Lunch Recipes -->
        <ListBox x:Name="LunchRecipeList"
                 Grid.Row="2" Grid.Column="0"
                 DisplayMemberPath="Name"
                 SelectionChanged="LunchRecipeList_SelectionChanged"
                 Margin="10"
                 FontSize="16"
                 VerticalAlignment="Stretch"
                 HorizontalAlignment="Stretch" />

        <!-- Recipe Details -->
        <StackPanel Grid.Row="2" Grid.Column="1" Margin="20,10,10,10">
            <TextBlock Text="Recipe Details" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <Border BorderBrush="DarkOliveGreen" BorderThickness="1" Padding="10" Background="Transparent">
                <TextBlock x:Name="LunchDetailsText"
               TextWrapping="Wrap"
               FontSize="16"
               Foreground="Black" />
            </Border>
            <Button Content="Add to Favorites"
                Width="150"
                Height="40"
                Margin="0,20,0,0"
                HorizontalAlignment="Left"
                Click="AddToFavorites_Click" />


        </StackPanel>
    </Grid>
</Window>
